{
  "name": "linkedin-translator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "English -> LinkedIn-speak translator running on Cloudflare Workers AI",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
